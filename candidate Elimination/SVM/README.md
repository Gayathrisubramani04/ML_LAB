# SVM Spam Detector UI

Streamlit UI for spam email detection using a support vector machine.

Dataset expected at `..\data\spamhamdata.csv` with tab-separated values:

```
label\ttext
```

## Run

```bash
pip install -r requirements.txt
streamlit run app.py
```
